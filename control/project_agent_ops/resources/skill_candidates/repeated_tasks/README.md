# Repeated Tasks

Canonical bucket for repeated task patterns that may deserve a dedicated skill or reusable workflow contract.

Use this directory when the same task shape appears across runs, agents, or operator sessions.

Typical sources:

- provider feedback that keeps repeating
- subagent retrospectives
- repeated task packets with the same workflow shape
- smoke runs that keep needing the same bounded preparation sequence
