# Progress

## 2026-03-26

- Created a fixed workspace layout with `docs`, `config`, `specs`, `checks`, `registry`, and `.codex-tasks`.
- Moved canonical files out of the repository root into stable directories.
- Found local workspace skills for dispatcher, worker, auditor, and commit manager. They existed but were still template placeholders.
- Replaced placeholder skill content with minimal operational guidance so the skills are inspectable and ready for use.
- Next step is to refresh the path registry and perform MCP wrapper smoke checks, including restart-needed notes.
