# NOTE_lean_ppt_portfolio_slice_direction-at2026-04-11-18-03

## Intent

Freeze the next near-term delivery slice as a lean PPT portfolio workflow instead of another broad parsing or platform program.

## Frozen Direction

The next active slice should be:

- `make a PPT portfolio that reflects the characteristics of the images already inside a PPT`

not:

- reopen a broad caption experiment
- build a new monolithic MCP first
- package a full Docker-first platform first

## Lean Boundary

The lean slice should:

1. use one primary source PPT only
2. reuse already-extracted image assets
3. treat image characteristics as slide-driving material, not as passive attachments
4. use the existing global `pptx` skill as the PPT authoring surface
5. use local workflow skills only as supporting consumers

## Source Selection

Primary source PPT for the lean slice:

- `02_1`
- rationale:
  - already contains UI screenshots, profile material, architecture/system diagrams, code/problem-solving artifacts, logos, and portfolio tables
  - better fit for a portfolio-first output than the evaluation-heavy `01_full_presentation_2026-03-17`

Secondary source PPT:

- `01_full_presentation_2026-03-17`
- role:
  - appendix or evidence-only material, not the first portfolio narrative source

## One-Line Summary

The next move is a lean, image-character-driven portfolio deck built from the existing `02_1` PPT asset set, using `pptx` for authoring and keeping the broader platform work deferred.
