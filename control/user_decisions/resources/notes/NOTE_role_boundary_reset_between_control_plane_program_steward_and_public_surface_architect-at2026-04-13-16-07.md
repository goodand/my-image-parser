# NOTE_role_boundary_reset_between_control_plane_program_steward_and_public_surface_architect-at2026-04-13-16-07

## Decision

Current role boundaries are reset as follows:

- `Control-Plane Program Steward`
  - owns experimental know-how promotion into reusable tool-facing artifacts
  - owns owner-family mapping, routing contracts, page-link matrices, regeneration handoff bundles, and machine-readable execution surfaces
  - owns the rule that local external-tool clones are linked as donor/reference surfaces without replacing workspace truth
- `Public Surface Architect`
  - owns readable public entry surfaces for other agents
  - owns review indexes, start-here navigation, public-facing cross-links, symbolic-link publication guidance, and public-surface git packaging
  - does not own tool contract changes, owner-family reinterpretation, MCP lifecycle changes, or script behavior changes

## Why

The current `lean 02_1` portfolio work already has:

- reusable machine-readable routing
- reusable regeneration handoff
- explicit skill and external-tool linkage

What is still missing is one clearer public surface that another agent can enter without reading every reference in sequence.

That gap belongs to `Public Surface Architect`, not to the toolization owner.

## Active Request Direction

If `Public Surface Architect` is engaged next, the request should be:

- create one `Start Here` public entry surface for the `lean 02_1` multimodal PPT stack
- link the current local truth surfaces, not just abstract plans
- preserve the current role boundary
- avoid rewriting contracts, scripts, or family ownership

## Non-Intent

- This note does not reopen skill creation.
- This note does not reopen MCP onboarding.
- This note does not move PPT into a new owner family.
- This note does not authorize deck redesign.

## One-Line Summary

`Control-Plane Program Steward` should keep converting experimental know-how into reusable tool contracts, while `Public Surface Architect` should package those results into a clean public entry surface that other agents can follow quickly.
