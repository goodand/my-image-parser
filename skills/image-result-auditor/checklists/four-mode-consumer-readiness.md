# Four-Mode Consumer Readiness Checklist

- confirm the resolved image set matches the current decision-level truth
- confirm no stale residual image remains in the consumed bundle set
- confirm the consumer can accept either aggregate input or per-image fallback
- confirm `qualitative_winner_candidate` and `recommended_current_default` are both reported
- confirm promotion state is preserved for parser and reviewed branches
- confirm semantic judge availability is stated explicitly
- confirm a waiver is written when no semantic judge exists
